{
  "name": "study",
  "version": "1.0.0",
  "private": true,
  "description": "Static web app deployed from the public directory.",
  "scripts": {
    "dev": "npx serve public",
    "build": "node -e \"console.log('Static app: no build step required')\""
  }
}
